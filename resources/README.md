# Resources Directory

Use this folder to cache downloaded models and datasets (e.g., via `uv run python -m synwoz resources --`).
Everything here is ignored by git except this README and the `.gitignore`, so you can store large assets without risking accidental commits.
